{
  "name": "@brainledge/server",
  "version": "0.1.0",
  "description": "Brainledge HTTP, MCP, and worker entrypoints.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@brainledge/core": "workspace:*",
    "@brainledge/storage": "workspace:*",
    "@hono/node-server": "^1.19.9",
    "@modelcontextprotocol/server": "2.0.0",
    "hono": "^4.9.8",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "24.10.1"
  }
}
